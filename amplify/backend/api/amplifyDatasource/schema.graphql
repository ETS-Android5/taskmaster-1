type Task @model @auth(rules: [{allow: public}]){
  id: ID!
  name: String!
  body: String
  dateCreated: AWSDateTime
  State: State
}

enum State
{
    New
    Assigned
    In_Progress
    Complete
}
